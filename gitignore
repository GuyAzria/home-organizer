# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
# __pycache__ is the important one. It is generated by `python -m compileall`
# and by Home Assistant itself at runtime, and if it ends up committed it gets
# shipped into every user's config directory through HACS.
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# ---------------------------------------------------------------------------
# Development Tools and Reports
# ---------------------------------------------------------------------------
reports/
tools/

# ---------------------------------------------------------------------------
# Tooling caches
# ---------------------------------------------------------------------------
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Output of tools/ha_compliance_report.py --json
report.json

# ---------------------------------------------------------------------------
# Editors and OS
# ---------------------------------------------------------------------------
# NOTE: .vscode/ at the REPOSITORY ROOT is intentionally NOT ignored - it holds
# the shared task definitions and is useful to everyone working on this repo.
# What must never exist is .vscode inside custom_components/, which would be
# installed onto every user's system. That was an explicit review rejection.
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# Local Home Assistant test instance
# ---------------------------------------------------------------------------
# If you run a dev instance inside this folder, none of its runtime state
# belongs in the repository.
config/
.storage/
home-assistant.log*
home-assistant_v2.db*
home-assistant.db*
*.log

# Runtime data this integration creates in the HA config directory
home_organizer.db
home_organizer_*_cache*.json
home_organizer_images/

# ---------------------------------------------------------------------------
# Release artefacts
# ---------------------------------------------------------------------------
*.zip
*.tar.gz

# ---------------------------------------------------------------------------
# Secrets - belt and braces. Nothing here should ever be committed.
# ---------------------------------------------------------------------------
secrets.yaml
.env
.env.*
*.pem
*.key
credentials.json
token.json